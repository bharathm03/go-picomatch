{
  "_comment": "Generated by tools/charaxis/generate.js. Inputs are chosen; every expected value is recorded from upstream picomatch. Not upstream's own tests — see testdata/original for those. Do not hand-edit.",
  "upstream": "4.0.5",
  "generatedBy": "tools/charaxis/generate.js",
  "cases": {
    "total": 150,
    "threw": 6,
    "byAxis": {
      "utf16-code-units": 34,
      "js-dot-exclusions": 24,
      "case-folding": 32,
      "maxlength-units": 14,
      "fastpaths": 24,
      "dot-guard-lexical": 22
    },
    "platforms": [
      "posix",
      "windows"
    ]
  },
  "axes": [
    {
      "axis": "utf16-code-units",
      "kills": "runes-not-code-units",
      "probes": 17,
      "note": "`?` matches one UTF-16 code unit, so one astral character is two."
    },
    {
      "axis": "js-dot-exclusions",
      "kills": "globstar-crosses-newline",
      "probes": 12,
      "note": "The globstar body uses JS `.`, which excludes exactly \\n \\r U+2028 U+2029."
    },
    {
      "axis": "case-folding",
      "kills": "unicode-case-folding",
      "probes": 16,
      "note": "nocase uses JS non-unicode Canonicalize, which refuses to fold non-ASCII onto ASCII. Go (?i) and unicode.ToLower both do fold them."
    },
    {
      "axis": "maxlength-units",
      "kills": "maxlength-in-code-points",
      "probes": 7,
      "note": "The cap counts UTF-16 units. Go len() counts bytes and len([]rune()) counts code points; for an astral string all three differ."
    },
    {
      "axis": "fastpaths",
      "kills": "no-fastpaths",
      "probes": 12,
      "note": "The inline fast path changes the compiled output, not just the route to it: it adds trailing-slash leniency the full scanner does not emit."
    },
    {
      "axis": "dot-guard-lexical",
      "kills": "(no mutation; upstream coverage is a single test)",
      "probes": 11,
      "note": "The leading-dot guard is emitted from the pattern's lexical left neighbour, not from the match position. Exactly one upstream test discriminates this."
    }
  ]
}
